"""Module docstring."""  # pylint: disable=too-few-public-methods
class InfrastructureMemoryGraph:
    """Class docstring."""

    def correlate(
        self,
        repos,
        containers,
        caches
    ):
        """Function docstring."""

        return {
            "repos": repos,
            "containers": containers,
            "caches": caches
        }
