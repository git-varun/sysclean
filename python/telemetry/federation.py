"""Module docstring."""  # pylint: disable=too-few-public-methods
class FederationRuntime:
    """Class docstring."""

    def aggregate(self, telemetry_nodes):
        """Function docstring."""

        return {
            "nodes": len(telemetry_nodes)
        }
